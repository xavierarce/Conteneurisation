name: sdn_lab

topology:
  nodes:
    dev-node1:
      kind: linux
      image: projet-ml-env:latest # Utilise l'image créée plus haut[cite: 1]
    dev-node2:
      kind: linux
      image: projet-ml-env:latest

  links:
    - endpoints: ["dev-node1:eth1", "dev-node2:eth1"]
